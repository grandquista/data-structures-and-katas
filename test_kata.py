from kata import kata
from re import compile as rc


def test_kata_1():
    i = 1
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_2():
    i = 2
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_3():
    i = 3
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_4():
    i = 4
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_5():
    i = 5
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_6():
    i = 6
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_7():
    i = 7
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_8():
    i = 8
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_9():
    i = 9
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_10():
    i = 10
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_11():
    i = 11
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_12():
    i = 12
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_13():
    i = 13
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_14():
    i = 14
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_15():
    i = 15
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_16():
    i = 16
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_17():
    i = 17
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j


def test_kata_18():
    i = 18
    test = rc(kata(i))
    for j in range(100):
        sample = bin(j)[2:]
        match = test.search(sample)
        assert match if j % i == 0 else not match, j
